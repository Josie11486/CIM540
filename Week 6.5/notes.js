angle=0;
function setup() {
  createCanvas(200, 200);
  background(204);
}

function draw() {

// push();
// translate(mouseX, mouseY);
// fill(255);
// ellipse(0, 0, 40, 40);
// // pop();
// fill(255, 0, 0);
// rect(100, 100, 40, 40);


  // translate(mouseX, mouseY)
  // rotate(angle);
  // //what do the first two numbers of the rect do?
  // rect(-30, -30, 60, 60);
  // angle += 10;

  // scale(3);
  // rect(20, 20, 40, 40);

}
